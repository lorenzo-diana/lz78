#include "lz78_compressor.c"#include "lz78_decompressor.c"#include <unistd.h>#include <stdio.h>#include <stdlib.h>/*	"cdi:o:s:v"	-c compress	-d decompress	-i <filename>	-o <filename>	-v verbose	-s <dictionary size>*/int main (int argc, char* argv[]){	extern char *optarg;	const char* option = "cdi:o:s:v";	int operation;	char cd;	char * input_filename = NULL;	char verbose='f'; 								//default false	int dic_size;	char * output_filename = NULL;	if (argc < 4)	{		printf("error\n");						//manca un parametro		return 0;	}	while((operation = getopt(argc,argv,option))!=-1)	{		switch(operation)		{			case 'c':								//compression						cd='c';			case 'd':								//decompression						cd='d';			case 'i':						input_filename=optarg;			case 'o':						output_filename=optarg;			case 'v': 								//verbose						verbose='t';			case 's':								//dictionary size						dic_size=atoi(optarg);			default :						printf("error\n");		//parametro non riconosciuto o errore						return 0;		}	}}